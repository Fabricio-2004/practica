apiVersion: v1 
kind: Pod
metadata:
    name: app-pod
    labels:
          app: app
          type: mvc
spec:
  containers:  
    - name: app-pod
      image: fabriciohs/busybox
      ports:
        - containerPort: 8080
